public class operators {
    public static void main(String[] args) {
        // Arithmetic: +, -, *, /, %
        // int num1 = 7;
        // int num2 = 5;
        // int result = num1 + num2;
        // int result = num1 - num2;
        // int result = num1 * num2;
        // int result = num1 / num2;
        // int result = num1 % num2;
        // System.out.println(result);

        //shorthand
        // int x = 5;
        // x = x + 5;
        // x += 2;
        // x++; // post increment
        // x--; // post decrement
        // ++x; // pre increment
        // --x; // pre decrement
        // x -= 2;
        // x *= 2;
        // x /= 2;
        // x %= 2;
        // System.out.println(x);



        // Relational: >, >=, <, <=, ==, !=
        // int p = 5;
        // int q = 7;
        // boolean res = p < q;
        // System.out.println(res);



        // Logical: &&, ||, ! -> combine multiple conditions
        // boolean condition = true && true;
        // boolean condition = true && false;
        // boolean condition = false && true;
        // boolean condition = false && false;
        // System.out.println(condition);

        // boolean con = true || true;
        // boolean con = true || false;
        // boolean con = false || true;
        // boolean con = false || false;
        // System.out.println(con);

        // boolean co = !true;
        // System.out.println(co);

        // Note: There is a difference between | and || , & and && if we use single symbol the both condition is checked to detect the output but in case of double symbol (i.e. && and || the shortcircuit) the second condition is ignored if the result can be given by only looking at the first example: true || true or false will always reslut in true



    }
}
